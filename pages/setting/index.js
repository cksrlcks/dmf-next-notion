import Setting from "../../components/setting";
export default function SettingPage({ changeSlideShowTime, autoTime }) {
    return <Setting changeSlideShowTime={changeSlideShowTime} autoTime={autoTime} />;
}
